apiVersion: v1
kind: Service
metadata:
  name: front-service
  labels:
    app: front-service    
spec:
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: front-pod
